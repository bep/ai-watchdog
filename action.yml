name: 'AI Watchdog'
description: 'Detect if a pull request was generated by AI'
author: 'Your Name'

inputs:
  github-token:
    description: 'GitHub token for API access'
    required: true
  openai-api-key:
    description: 'OpenAI API key'
    required: true

runs:
  using: 'node16'
  main: 'dist/index.js'

branding:
  icon: 'eye'
  color: 'blue' 